import java.util.*;
class Olmpiyad
{
    /*public static boolean palindrome(String s)
    {
        String let="";
        for(int x=0;x<s.length();x++)
        {
            let=s.charAt(x)+let;
        }
        return let.equals(s) ? true : false;
    }
    static String suram;
    static int count =0;
    public static void anagram(String fix,String s)
    {
        if(s.length()!=1)
        {
            for(int x=0;x<s.length();x++)
            {
                String t1=s.substring (0,x);
                String t2=s.substring(x+1);
                anagram(fix+s.charAt(x),t1+t2);
            }
        }
        else
        {
            suram = fix+s;
            //if(palindrome(suram))

            System.out.println(suram);
            count++;

        }
    }

    public static void main(String args[])
    {
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter a word");
        String w=sc.next();   
        for(int x=0;x<s.length();x++)
            anagram("",s.substring(0,x));
        System.out.println(count);
    }

    public static String subString(String s,int x)
    {
        return 
    }*/
}