package Inhertiance.Dynamic_Binding;

interface Shape
{
    int n=5;//it is final and as well as static if not declared
    public void input();
    public double area();
    //There cannot be any instant variables//abstract can have instant variable
    //instant methods can't have bodies//abstract methods can have body
}